# Development and local setup

This page is part of the [Frontend (Nuxt 3) guide](README.md).

## Dependencies
- `pnpm install` — installs dependencies (run inside `frontend/`).

## Running the dev server
- `pnpm dev` starts the Nuxt 3 dev server with hot reload.
- Nuxt listens on `http://localhost:3000` by default.
