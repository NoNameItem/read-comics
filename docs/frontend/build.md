# Build, preview, and linting

See the [Frontend (Nuxt 3) guide](README.md) for the broader context.

## Build
- `pnpm build` — creates a production build of the Nuxt 3 app.
- `pnpm preview` — serves the built app locally for verification.

## Linting
- `pnpm lint`
- `pnpm lint:check`
