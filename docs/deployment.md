# Deployment and releases

## Infrastructure
- Docker stacks: `compose/` and the root `local.yml`.
- Deployment scripts: `deploy/`.

## Release process
- Release Please config: `release-please-config.json`.
- Changelog: `CHANGELOG.md`.

## Open items
- Document staging and production steps in detail.
- Describe CI/CD pipelines and entry points.
