# Powers

Keeps a catalog of superpowers and their classifications.

## Key modules
- `models.py` lists abilities, categories, and associations with characters.
- `search_adapters.py` and `api/` provide search and serialization layers for power data.
- `tasks.py` drives updates and imports from external sources.
- `views.py` and `templates/` render public-facing power reference pages.
