# Characters

Stores character profiles, relationships, images, and search integration.

## Key modules
- `models.py` defines characters, teams, and story arc relationships.
- `search_adapters.py` and `api/` provide search indexing and serializers/endpoints.
- `tasks.py` performs imports and updates from external data sources.
- `templates/` and `views.py` render character cards and reference lists.
